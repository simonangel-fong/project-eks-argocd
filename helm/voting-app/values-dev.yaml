namespace:
  create: true
  labels:
    istio.io/dataplane-mode: ambient

image:
  pullPolicy: Always
  tag: sha-ca490bf

gateway:
  enabled: true
  createGateway: false
  className: istio
  host: voting.arguswatcher.net
  parentRef:
    name: istio-ingress
    namespace: istio-ingress

postgres:
  persistence:
    size: 2Gi

metrics:
  enabled: false
